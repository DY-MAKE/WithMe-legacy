# variables:
# DOCKER_HOST: tcp://localhost:2375

# variables:
#   DOCKER_DRIVE: overlay2

# services:
#   - docker:dind

stages:
  - build

# image:
#   name: docker/compose:latest

before_script:
  - docker version

build:
  stage: build
  tags:
    - build
  script: "docker build -t gitlab.dyhs.kr/withme ."
